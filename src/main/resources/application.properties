server.port=8082

# Issue - Potential Sensitive Information Disclosure (KEEP for vulns demo)
logging.level.com.example.vulnspring=TRACE


spring.datasource.url=jdbc:mysql://localhost:3306/vulnspring?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=123456
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JWT
app.jwt.refresh-token-expiry=86400
jwt.secret=mySecretKeyThatIsAtLeast32CharactersLongForHMAC51212345678

# Session security (uncomment for production, comment for vulns)
# server.servlet.session.cookie.http-only=true
# server.servlet.session.cookie.secure=true